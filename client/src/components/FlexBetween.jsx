import { Box, styled } from "@mui/material";

const FlexBetween= styled(Box)({
    display: "flex",
    alignItems: "center",
    justifyContent: "flex-start"
});

export default FlexBetween
//for flexed components to be used in the pages